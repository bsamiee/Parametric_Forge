{
    "strictness": 2,
    "commentTypes": ["logic", "syntax"],
    "triggerOnUpdates": true,
    "triggerOnDrafts": true,
    "statusCheck": true,
    "statusCommentsEnabled": true,
    "fixWithAI": true,
    "shouldUpdateDescription": false,
    "updateSummaryOnly": false,
    "hideFooter": false,
    "labels": [],
    "disabledLabels": [],
    "includeAuthors": [],
    "excludeAuthors": [],
    "includeBranches": [],
    "excludeBranches": [],
    "includeKeywords": "",
    "ignoreKeywords": "",
    "disabledRules": [],
    "summarySection": {
        "included": true,
        "collapsible": true,
        "defaultOpen": true
    },
    "issuesTableSection": {
        "included": true,
        "collapsible": true,
        "defaultOpen": true
    },
    "confidenceScoreSection": {
        "included": true,
        "collapsible": true,
        "defaultOpen": true
    },
    "sequenceDiagramSection": {
        "included": true,
        "collapsible": true,
        "defaultOpen": false
    },
    "ignorePatterns": "flake.lock\npnpm-lock.yaml\n*.wasm\nmodules/home/assets/**\n.claude/scratch/**",
    "context": {
        "repos": ["bsamiee/Rasm", "bsamiee/Maghz"]
    },
    "rules": [
        {
            "id": "greenfield-only",
            "rule": "No compat shims, wrapper modules, aliases for retired names, migration helpers, or anticipatory code. A rename replaces every call site in the same change.",
            "severity": "high",
            "enabled": true
        },
        {
            "id": "one-owner-per-axis",
            "rule": "Extend the canonical owner: each vocabulary axis (color, keybind, MCP server, tool admission) has exactly one declaring owner per docs/standards/nix-doctrine.md, and new capability lands as a row on that owner. A new singleton file or duplicated value for an owned axis is a defect.",
            "severity": "high",
            "enabled": true
        },
        {
            "id": "shell-app-bar",
            "rule": "Shell bodies — packaged via writeShellApplication or standalone .sh including .claude hooks — declare runtimeInputs where packaged, use set -euo pipefail, pass shellcheck without directives, carry zero '|| true' masks, and embed no repo paths, usernames, or magic values.",
            "severity": "medium",
            "scope": ["modules/**", "overlays/**", "flake-modules/**", ".claude/**"],
            "enabled": true
        },
        {
            "id": "provision-contract",
            "rule": "forge-provision emits schema-v3 JSON envelopes only; agent-facing JSON never carries socket paths, host absolute paths, or token material; every envelope change lands with its README contract row; jq programs live in jq/ files, never inlined in Bash.",
            "severity": "high",
            "scope": ["overlays/forge-provision/**"],
            "enabled": true
        },
        {
            "id": "services-effect-rails",
            "rule": "services/ TypeScript follows docs/stacks/typescript law: Effect rails, tagged faults, Config.redacted, Schema boundary decodes, typed Pulumi rows, secrets never in unredacted state outputs. Dependency movement is forward-only: a downgrade or stale pin without a recorded reason is a defect.",
            "severity": "high",
            "scope": ["services/**"],
            "enabled": true
        },
        {
            "id": "generated-projection",
            "rule": "Lua and TOML app config consumes Nix-owned vocabulary projections; a private hex color, ad hoc keybind, or duplicated theme value in consumer config is a defect — the value belongs on the generating owner.",
            "severity": "medium",
            "scope": ["modules/home/programs/**"],
            "enabled": true
        },
        {
            "id": "workflow-determinism",
            "rule": "Workflow scripts are deterministic orchestration: schema-typed agent outputs, bounded loops, stage products written to disk with path receipts. Bare promises, unbounded polling, wall-clock branching, and subagents instructed to wait are defects.",
            "severity": "medium",
            "scope": [".claude/workflows/**"],
            "enabled": true
        }
    ]
}
