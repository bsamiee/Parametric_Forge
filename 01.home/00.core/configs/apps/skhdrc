# Title         : skhdrc
# Author        : Bardia Samiee
# Project       : Parametric Forge
# License       : MIT
# Path          : /01.home/00.core/configs/apps/skhdrc
# ----------------------------------------------------------------------------
# Unified hotkey configuration with conflict-free hierarchy

# --- BLACKLIST: Applications that should not receive skhd hotkeys -----------
# These apps handle their own keyboard shortcuts or need full keyboard access
.blacklist [
"1Password 7"
"secure keyboard entry"
"Activity Monitor"
"System Settings"
"System Preferences"
]

# --- TIER 1: Daily Operations (80% usage - Simple & Memorable) -------------
# Window focus with intuitive arrow keys - no app conflicts
opt - left : [
"WezTerm" ~ # Let WezTerm handle its own pane navigation
"Code" ~    # Preserve VS Code navigation
"Visual Studio Code" ~
"IntelliJ IDEA" ~
"*" : yabai -m window --focus west
]

opt - down : [
"WezTerm" ~
"Code" ~
"Visual Studio Code" ~
"IntelliJ IDEA" ~
"*" : yabai -m window --focus south
]

opt - up : [
"WezTerm" ~
"Code" ~
"Visual Studio Code" ~
"IntelliJ IDEA" ~
"*" : yabai -m window --focus north
]

opt - right : [
"WezTerm" ~
"Code" ~
"Visual Studio Code" ~
"IntelliJ IDEA" ~
"*" : yabai -m window --focus east
]

# Display focus (unchanged)
alt - s : yabai -m display --focus west
alt - g : yabai -m display --focus east

# Workspace navigation - stable numbers that never change
shift + ctrl + alt - 1 : yabai -m space --focus 1
shift + ctrl + alt - 2 : yabai -m space --focus 2
shift + ctrl + alt - 3 : yabai -m space --focus 3
shift + ctrl + alt - 4 : yabai -m space --focus 4
shift + ctrl + alt - 5 : yabai -m space --focus 5
shift + ctrl + alt - 6 : yabai -m space --focus 6
shift + ctrl + alt - 7 : yabai -m space --focus 7
shift + ctrl + alt - 8 : yabai -m space --focus 8
shift + ctrl + alt - 9 : yabai -m space --focus 9

# --- TIER 2: Essential Manual Overrides (SHIFT + OPT) ---------------------
# Core toggles when you need manual control over auto-balance
shift + opt - return : yabai -m window --toggle zoom-fullscreen  # Toggle fullscreen
shift + opt - space  : yabai -m window --toggle float --grid 4:4:1:1:2:2
shift + opt - r      : yabai -m space --rotate 270  # Fix layout when messy

# --- TIER 3: Power Operations (MEH = shift+alt+opt) -------------------------
# Move window to workspace and follow (stable workspace numbers)
shift + alt + opt - 1 : yabai -m window --space 1; yabai -m space --focus 1
shift + alt + opt - 2 : yabai -m window --space 2; yabai -m space --focus 2  
shift + alt + opt - 3 : yabai -m window --space 3; yabai -m space --focus 3
shift + alt + opt - 4 : yabai -m window --space 4; yabai -m space --focus 4
shift + alt + opt - 5 : yabai -m window --space 5; yabai -m space --focus 5
shift + alt + opt - 6 : yabai -m window --space 6; yabai -m space --focus 6
shift + alt + opt - 7 : yabai -m window --space 7; yabai -m space --focus 7
shift + alt + opt - 8 : yabai -m window --space 8; yabai -m space --focus 8
shift + alt + opt - 9 : yabai -m window --space 9; yabai -m space --focus 9

# Window swapping 
shift + alt + opt - left  : yabai -m window --swap west
shift + alt + opt - right : yabai -m window --swap east
shift + alt + opt - down  : yabai -m window --swap south  
shift + alt + opt - up    : yabai -m window --swap north

# Display management
shift + alt + opt - s : yabai -m window --display west; yabai -m display --focus west
shift + alt + opt - g : yabai -m window --display east; yabai -m display --focus east

# --- TIER 3: Expert Overrides (SUPER = shift+alt+opt+cmd) ------------------
# When auto-balance isn't enough - rare usage
shift + alt + opt + cmd - left  : yabai -m window --swap west
shift + alt + opt + cmd - right : yabai -m window --swap east  
shift + alt + opt + cmd - x     : yabai -m space --mirror x-axis
shift + alt + opt + cmd - y     : yabai -m space --mirror y-axis

# --- Utility Operations (Simple Keys) ---------------------------------------
# Quick navigation and space management
alt - tab : yabai -m space --focus recent || yabai -m space --focus first
shift + alt + opt + cmd - c : yabai -m space --create && index="$(yabai -m query --spaces --display | jq '.[-1].index')" && yabai -m space --focus "${index}"
shift + alt + opt + cmd - d : yabai -m space --destroy

# --- Service Management -----------------------------------------------------
# Nix-managed LaunchD services (not Homebrew)
ctrl + alt - q : launchctl unload ~/Library/LaunchAgents/org.nixos.yabai.plist
ctrl + alt - s : launchctl load ~/Library/LaunchAgents/org.nixos.yabai.plist
ctrl + alt - r : launchctl kickstart -k gui/$(id -u)/org.nixos.yabai
