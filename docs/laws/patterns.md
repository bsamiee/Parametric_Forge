# [PATTERNS]

Pattern law binding two or more of this repo's language branches — Nix, Bash, TypeScript, Lua — that no law page charters. `design.md` and the machine law pages own cross-branch executable law, so a row admits here only with proof of absence across them, binds every branch it names, and moves to the owning law page the moment one charters its family; a single-branch law routes to that branch's stack page or law page instead.
